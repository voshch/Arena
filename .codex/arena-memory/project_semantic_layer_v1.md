---
name: semantic-layer-v1
description: "Semantic annotation layer v1 on feature/semantic (metarepo + arena_evaluation submodule), live-benchmark-accepted, jazzy merged in 2026-08-09; pending fast-forward/land to jazzy"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3553827a-da09-4bfe-b7d6-44750c656f54
  modified: 2026-08-10T03:10:56.105Z
---

Semantic annotation layer v1 (Matterix-inspired, own spec) COMMITTED 2026-07-19: metarepo `c6c7ac0` "world semantics" on `feature/semantic`, arena_evaluation submodule `3a8304a` "record world semantics" on its own `feature/semantic` (pin bumped). Unpushed, unmerged to jazzy.

Frozen spec (C1-C5, D1, D2, reserved M2-M4 namespaces): `/tmp/claude-1000/-home-arena-arena-ws-src-Arena--meta-repos/3553827a-da09-4bfe-b7d6-44750c656f54/scratchpad/SPEC.md` (session scratchpad, may not survive tmp cleanup).

Key shipped facts: kinds = state + predicate only (no transition), BDDL-flavored names, `semantics:` on Door/Elevator/Zone (zones inert, published node-side), cfgs in `arena_simulation_setup.shared.semantics` re-exported via `task_generator.shared`, registry + `SemanticsManager` in `arena_runtime/sim/_semantics.py` (no task_generator_msgs import, plain-Python snapshots), one-line wrap in `_tick`, re-home via FSM field mutation + `last_applied_progress` deferral (D2: `move_box` is fire-and-forget, tick loop frozen while paused), topics `state/semantics` (latched) + `state/semantic_events` (depth-20 TL) on task_generator_node, msgs SemanticEntityState/Snapshot/Event, W5 six-chokepoint recorder wiring + `semantic_interaction_metrics` (time_waiting_at_doors, elevator_rides). Bool wire format: lowercase "true"/"false", W5 parses case-insensitively. `test_mechanism_shim.py` byte-identical (parity proof). Realizer carries semantics for free via attrs.evolve. Fixed pre-existing bug: door/elevator FSM state was never re-homed across episodes.

LIVE TEST PASSED 2026-07-19 (gazebo headless, three_storied_residential): annotated world parsed, door + elevator entities in state/semantics exactly per spec C1 (reset values, index-aligned arrays, env_0/<name>/<level> naming), semantic_events flowing with the wire contract (empty previous on first emission, field_kind constants). Level 1 of worlds/three_storied_residential got `semantics: [{preset: door|elevator}]` annotations as test content, uncommitted, keep-or-revert at commit gate. Worlds are read from the INSTALL share (ASS_DIR) but files are symlink-installed so source edits are live; a running env only re-reads world.yaml on world change, which rides EpisodeRecord/queue overrides, not the world param.

M1+M2 COMMITTED 2026-07-19: metarepo `0b4f3cc` "scriptable semantics" (pin bumped), arena_evaluation `46941b0` "compliance + regime metrics", both on feature/semantic, unpushed. M1+M2 LIVE-SMOKE-PASSED 2026-07-19 (hospital_1 headless gazebo: 11 doors + 11 zones in snapshot with exact annotated values, members on wire, semantics/set full error contract + zone override write propagating to the latched snapshot). M1+M2 IMPLEMENTED 2026-07-19 (8 agents + review, staged uncommitted on both feature/semantic branches): M1 compliance metrics (speed/quiet/restricted zones + doorway blocking, zone geometry from world asset at processing time, elevator etiquette DEFERRED to per-occupant identity), hospital_1 + reception annotated, compliance benchmark suite. M2: five kinds (signal, schedule, gate, pressure_plate, occupancy_cap), WRITABLE/set_value write path via semantics/set service with three-writer authority, scenario timelines (at/every/when, every first fires at offset+every, never t0), regime bus + three FSM hook wraps (parity suite untouched), F2 quantization (progress 0.1), members on snapshots (wire change approved), cabin_door via elevator_full preset, Schedule/Signal entities in shared/world.py (NOT tree/World). Specs SPEC_M1/SPEC_M2 in same scratchpad. DEFERRED: fire-alarm world-side wiring + live demo (scenario parse-tested only), multi-level metric name-keying (latent, reviewer-flagged), ped compliance (needs humansim approval).

HISTORY REWRITTEN twice 2026-07-20 (user-approved amend of older-bug fixes into unpushed commits, manual cherry-pick chains both repos). FINAL SHAs: metarepo ab96fcd world semantics / 9bc67e6 scriptable semantics / e1b9673 episode conditions / c96ee85 fix benchmark runner (pin bump); arena_evaluation d1da650 record world semantics (+mcap EndOfFile tolerance) / a6b1367 compliance + regime metrics (+resolve_robot_pose pre-slice anchor fix, start_pos guards, env_offset threading from tf_static map->env_n/map + zone/door geometry translation, three_storied density fix, test_pose_resolution) / fdf1685 condition compliance (+pose_valid gating, ped stream split arena_peds-preferred with agent_states peds_fallback, parquet_store fallback, conditions suite stage file:"conditions", bounded scenarios/conditions/scenario.yaml in hospital_1 with the 5-clause block, flow scenario left pristine) / 62065fd fix benchmark runner (drop dead robots field, bound cancel awaits with _await_or_env_died). All prior SHAs DEAD, all unpushed so rewrites were safe. Post-surgery: 471+336+115+152 tests green, ruff clean (run ruff from Arena root only).

CRITICAL FRAME FACT discovered 2026-07-20: recorded GT robot poses AND arena_peds positions are in the GLOBAL map frame = env-local world coords + env packing offset, published as tf_static `map -> env_<n>/map` (hospital_1 env_0: (5,5)). world.yaml zone/door geometry is env-local. Evaluation translates geometry by AlignedEpisodeBundle.env_offset (from tf_static, (0,0) fallback when absent/ambiguous). The ladder (4.75,4.75) memory is this same offset. Multi-env recordings: _env_offset returns (0,0) if more than one env transform (open item).

EVICTION ROOT CAUSE gazebo benchmark: flow scenario source/sink crowds (max_total 50/episode) build spawn-service backlog -> tg heartbeat starves -> arena_node silently evicts env (clean SIGINT exit, "disappeared from /arena/state/envs") -> half-dead env wedges gz world services -> respawn ACTIVE timeout cascade. Bounded scenarios avoid it; eviction visibility + backlog fix remain open (same family as isaac heartbeat eviction memory). Benchmark runner now survives env death (bounded cancel + respawn-on-systemic verified live: 20+ cancels, zero wedges in run 4).

W5+M1+M3 LIVE ACCEPTANCE PASSED 2026-07-20: final benchmark run 20260720-011750-compliance-basic, ALL 4 stages ran to completion (33 episodes, zero env deaths, ~85 min), processed clean. condition verdicts with correct frames: ep1 0.0 (missed pharmacy, 3 passed/1 failed/1 unknown), eps2-3 UNKNOWN (4 robot clauses TRUE, ped clause unknown = precedence live). Compliance/semantic/regime columns populated per stage (door wait 226s hospital, 243s reception; replan medians ~0.1-0.2s). three_storied 0/10 successes = DWB inability, planner-side. Step verdicts read "failed" under the runner's strict episode-failure policy even with 5/10 successes, labeling only.

REVIEW FIXES + AMEND COMPLETE 2026-07-20 (all 6 adversarial findings + terminology deslop, WART RESOLVED). EVAL fixes were AMENDED into originals in the prior round (unchanged, still valid): eval feature/semantic = bf47ead record world semantics / 58a8986 compliance+regime (finding 3: _env_offset None on multi-env + skip-guards) / b4e087a condition compliance (finding 2: _entity_roster multi-kind by-name+kind-by-field) / ec97aef fix benchmark runner. METAREPO fixes NOW AMENDED into originals too (the earlier follow-up 5d29f2e/02c1c3a is GONE): rebuilt feature/semantic via pure plumbing (temp GIT_INDEX_FILE + commit-tree, no working-tree checkouts, no cherry-pick) which sidestepped the double-submodule cascade that corrupted 3 prior attempts. New metarepo SHAs: 9399cb2 world semantics (eval=bf47ead) / 79a7623 scriptable semantics (eval=58a8986, carries finding 6+7) / c01fc69 episode conditions (eval=b4e087a, carries finding 4+5) / 0e0709d fix benchmark runner (eval=ec97aef) / tip 02e1901 "Merge branch jazzy" (Vova's isaac 753dcfc merge, faithfully RECREATED with his author/date + 3616be2 as 2nd parent). ALL FOUR ORPHAN eval pins (d1da650/a6b1367/fdf1685/62065fd) now unreachable from the tip - zero orphan pins remain. Finding ownership: 6 (universal reset_mechanisms, ruling: all doors/elevators reset per episode for determinism) + 7 (real _tick door+elevator parity tests) fold into 79a7623; 4 (EpisodeCondition __attrs_post_init__ closes Parseable-fallback bypass) + 5 (Scenario.load raises on modern-key files) fold into c01fc69. TERMINOLOGY DESLOP (user "rehome as terminology? slop, should be reset"): home_mechanisms->reset_mechanisms, _home_door->_reset_door, docstrings/comments re-home->reset, test names *_rehomes_*->*_restores_* across test_semantics.py/test_semantics_m2.py/test_mechanism_shim.py; grep-verified zero home/rehome left in arena_runtime. Gate proof: `git diff 02c1c3a 02e1901` = ONLY the 5 renamed files (24/24 balanced), both submodule pins byte-identical old-vs-new tip. backup/semantic-pre-amend = 02c1c3a (safety net, keep until pushed). 156 arena_runtime tests green, ruff clean from Arena root. b4cd11b (Vova's merge) was preserved-by-recreation. eval repo history UNTOUCHED this round.

ZONE-BUG FIX AMENDED 2026-07-20 (supersedes ALL SHAs above). Adversarial review found a live bug in the shipped M3 checker: `ConditionComplianceCalculator._load_world` reused compliance's `_extract_zone_geometry`, which DROPS any zone lacking a max_speed/quiet/restricted annotation (compliance_metrics.py `if not has_annotation: continue`), so every `<subject> in <zone>` membership atom on a plain zone scored silent UNKNOWN (exactly DUCANH's `ped in ambush_zone` case; likely also explains the live-acceptance ped-clause UNKNOWN, not only the ped-name gap). FIX: `_extract_zone_geometry(flattened, require_annotation=True)` gains an opt-out; `_load_world` calls it with `require_annotation=False`; compliance callers unchanged by the default. Regression test `test_extract_zone_geometry_keeps_unannotated_when_not_required`. Amended into the condition-compliance commit via plumbing (same temp-index/commit-tree method), cascaded a re-pin through the metarepo. CURRENT tips: eval feature/semantic = bf47ead / 58a8986 / 7546a5d condition compliance (+zone fix) / 6cd8bd1 fix benchmark runner (TIP). metarepo feature/semantic = 9399cb2 world / 79a7623 scriptable / 3595d3d episode conditions / c766b95 fix benchmark runner / 8d52448 Merge jazzy (TIP, pins eval 6cd8bd1, isaac 753dcfc). Backups: backup/pre-zonefix in BOTH repos. Gates: eval diff = only the 3 fix files, metarepo diff = only the arena_evaluation pointer; 54 eval tests + ruff clean.

ADVERSARIAL REVIEW 2026-07-20 (fresh Opus agent, findings cross-checked). NEW real bugs found beyond known gaps: (A) `_entity_roster` in condition_metrics.py drops any bare entity name mapping to >1 (sim_path,kind) pair, so a door co-annotated with gate loses ALL its atoms (even door.open) to UNKNOWN, and stores a single arbitrary kind for reset-default lookup; latent (no world co-annotates yet), fix = key by name + select kind by the atom's field. (B) Parseable.try_parse cattrs fallback (arena_simulation_setup utils/cattrs.py) catches EpisodeCondition.parse exceptions and lenient-structures anyway, so malformed clauses (binary op w/o q etc.) are silently accepted at author time and rejected only by readers; author-time validation neutered. (C) ScenarioView.load() legacy fallback silently drops the ENTIRE conditions+timeline+regions block when one clause has an unknown op (Literal backstop throws -> load_legacy never reads them); pre-existing footgun M2/M3 inherit. Latent risks: odom-fallback + world-local start_pos double-offsets zones on non-tf_gt sims (isaac/dummy); ped ZOH is actually bounded to 100ms by aligner join_asof tolerance (spec claims unbounded); params-dependent reset defaults (signal/gate state) depend on the seed event landing inside [start,end] window. CORRECTED my own overstated claims: (1) test_mechanism_shim.py is byte-identical = NOT changed, and it calls FSM primitives directly, BYPASSING the _tick hook insertions, so the "parity suite proves the wrap safe" claim is FALSE; wrap is inspection-verified only, needs a real with-vs-without-semantics parity test. (2) reset_semantics re-homes ONLY annotated doors/elevators to CLOSED each episode while bare WORLD-layer doors carry state -> divergence gated on annotation; whether re-home is a fix or a regression depends on intended WORLD-layer door-state semantics (CLAUDE.md says WORLD layer survives resets) = USER DECISION PENDING. Confirmed CORRECT independently: env_offset sign/direction (473/7541 robot samples in pharmacy with offset, 0 without), precedence, no cache poisoning, start_pos gating, ped stream split, runner cancel fix. GIT INCIDENT: review subagents checked out jazzy/base in both repos and left working trees parked there (files vanished from disk); recovered via `git checkout feature/semantic` both repos + `git submodule update --checkout arena_isaac`, verified clean + 38 tests green. Branch pointers never lost the work.

PED NAME GAP (open): under gz+arena_humansim engine, arena_peds Pedestrian.name is numeric aid (\"1\",\"2\",\"3\") for scenario peds because adapter _agent_names never gets filled on the engine full-sync path (registration only in _spawn_dynamic_obstacles_impl and flow branch; the converter _agent_states_to_pedestrians falls back to str(agent_id)). Named ped atoms (ped_1) score UNKNOWN under this backend. Fix requires wiring aid->sim_path registration on the actual spawn path used (humansim-adjacent, approval-gated). EpisodeRecord start field also unreliable (robots_params fallback chain in episode_splitter), only gates pose_valid since GT poses dominate.

FIRST BENCHMARK RUN 2026-07-20 (compliance/basic, wedged but E2E-validating): conditions wire path PROVEN live (flow scenario conditions -> EpisodeRecord -> mcap -> splitter -> ConditionComplianceCalculator -> parquet condition_success/clauses_* populated, entity atoms replay real door events, entity roster exact). Uncommitted fixes from the run: runner.py drop `req.robots=[]` (QueueEpisode field removed by 07cf6ef, exposed by msg rebuild), mcap_reader _iter_messages_tolerant (EndOfFile on truncated recordings), compliance.yaml hospital_1_conditions stage (episodes 3, scenario flow), flow scenario conditions block. DISCOVERED OPEN ISSUES: (1) benchmark runner wedges after env dies mid-episode (cancel await on dead action server, ensure-hang family, run 20260719-222025 stage 1 only); (2) resolve_robot_pose teleport-slice picks longest segment + rebases to start_pos -> absolute-frame zone tests (M1 compliance + M3 zone atoms) invalid when start_pos missing or segment start != episode start (tonight: raw odom frame, all zone membership zero, quiet_zone_dwell bogus); (3) humansim agent_states stream carries numeric agent_id only -> named ped atoms (ped_1) UNKNOWN under humansim engine, needs aid->name mapping (engine-side, approval gate) or scenario-ped stream recording. Clean full-suite run still pending as the real W5+M1 acceptance.

M3 COMMITTED 2026-07-20: metarepo `b3ac650` "episode conditions" (pin bumped), arena_evaluation `0681f49` "condition compliance", both feature/semantic, unpushed. All container tests green (468+3 ass, 336 tg after task_generator_msgs rebuild, 110 eval incl. 35 new). Aligner backward join_asof = the ped ZOH, no extra plumbing. Caveat: conditions wire path unit-verified per piece, no live E2E episode yet (rides first benchmark run with W5). M3 RESCOPED 2026-07-19: retitled "verifiable episode conditions" (VLABENCH arm dead, language half M3-C generator + M3-D suite SHELVED in SPEC_M3). Approved calls: wire field named `conditions` (EpisodeRecord append, JSON list of {op,p,q,text}), precedence FALSE>UNKNOWN>TRUE, ped zone-membership atoms IN (`<subject> in <zone>`, subject robot|bare ped name, zero-order hold onto odom axis, peds_names column new in mcap_reader: Pedestrian.name / str(agent_id)), bare-name resolution (authored atoms env-stripped, checker strips env_<n>/ prefix, exact single match else UNKNOWN). Frozen grammar API in arena_simulation_setup.shared.conditions (EntityAtom, MembershipAtom, parse_atom, EpisodeCondition). Calculator = ConditionComplianceCalculator "condition_compliance" in condition_metrics.py, keys condition_success + clauses_total/passed/failed/unknown. Two agents: M3-A arena_evaluation (Opus), M3-B metarepo (Sonnet). Primary consumer: DUCANH Ambush hazard-materialization validity filters (decision: deliver feature/semantic for that paper as adopted infra, pre-registration window is the deadline).

JAZZY MERGED IN 2026-08-09 (user-directed direction flip: jazzy INTO feature/semantic, not the reverse). Eval submodule: feature/semantic merged jazzy clean (9 jazzy commits: benchmark runner/config/recorder/setup churn), tip `caa6b86`. Metarepo: feature/semantic merged jazzy (57 commits since fork), tip `363d0ed`, pins eval caa6b86. Only conflict was World.py Level class add/add (jazzy `wall_material` + semantic `semantics` field, both kept, semantics last) plus the eval pin. Gates: ruff clean from Arena root, claude-grep clean both merge diffs, zero unresolved paths. Landing to jazzy is now a fast-forward-shaped merge (jazzy is ancestor of feature/semantic tip); still unpushed, still user's call. Isaac SUPPORTED_SIMS claim (2b) still unverified, user will test.

SHAPE C COMMITTED 2026-08-09 as "restruct semantic and prep for release": eval 4fa53a6, metarepo 5950a33 (pins eval 4fa53a6). User-approved contract change "chasing minimum entropy": semantic wire collapsed to snapshot-only. Rationale established by audit: NOTHING consumed SemanticSnapshot offline (all calculators read events), yet the snapshot republished on every change batch, so the two channels were informationally redundant; worst entropy was eval's hand-mirrored _RESET_DEFAULTS (kind,field) table. New contract: single latched state/semantics topic republished on quantum-passing change/attach/detach/reset/write; SemanticEvent.msg + state/semantic_events DELETED; _diff/SemanticChange survive runtime-side as the republish gate only. Eval reconstructs: mcap_reader flattens snapshots to long format (time_ns, entity, kind, field, field_kind, value_str/value_num/value_bool, value_list sentinel field __members__) with forced pyarrow schema _SEMANTIC_SNAPSHOT_SCHEMA (all-null batches otherwise infer null type and break ParquetWriter — verified empirically); episode_splitter._episode_snapshot = in-window rows + seed (latest stamp <= start, else first in-window); _reconstruct_events in compliance_metrics.py (imported by semantic/condition/regime metrics) collapses series to change points re-stringified to the old event convention so replay logic survived unchanged; _RESET_DEFAULTS DELETED, _value_at holds earliest-known before seed. Implemented by 2 parallel sonnet agents (runtime 3 files -32 lines pure deletion; eval 17 files +495/-307). Gates green: 123 eval + 156 runtime tests in-container, ruff clean from root, zero SemanticEvent|semantic_events hits workspace-wide, claude-grep clean. Known edges: internal stringify convention retained (eval-internal now, not wire); pre-seed clamp only matters if recorder missed the reset snapshot. FINAL SHAs 2026-08-10: metarepo 5950a33 AMENDED to `76af189` (mechanism disc-gate work folded in, see [[project_mechanism_disc_gate]]; 14 files +358/-205, message unchanged), then `6f0eedc` "simplify sim interface" on top. Eval stays 4fa53a6. Container overlay verified current (SemanticSnapshot imports, SemanticEvent gone). Live-validated: elevator boarding + isaac both pass (user, 2026-08-10). Wire-invariant tests added in test_mechanism_shim.py (blocked-close publishes no closing/no flap, ride events deduped and bounded). INTRINSIC SEMANTICS + PUBLISH THROTTLE amended in 2026-08-10 (user: "we're not v1 anymore", blanket authority): every spawned door/elevator publishes its FULL vocabulary because it exists - `if not cfgs` gate and cfg field-subsetting deleted, presets door/elevator/elevator_full deleted, cabin_door(+progress) always on, recall_on promoted to first-class Elevator field (SemanticsManager.set_recall replaces the USES_PARAMS params-merge), environment_manager `_route_semantics` raises on host-vocab/unknown names, 14 redundant preset annotations stripped from the 3 worlds. `semantics:` now ONLY authors scripted kinds (gate/plate/signal/schedule/occupancy_cap) and zone values. Publish throttle: node's timeline loop renamed `_run_tick_loop` (_SIM_TICK_RATE 30.0 = gz max_step_size 0.0333 = MECHANISM_TICK_RATE), change/write paths set `_semantics_dirty`, loop flushes at most one snapshot per sim step, world-boundary publishes stay immediate (world_manager_ros direct call) and clear the flag. Per-mechanism topics REJECTED finally (stale latches on detach, recorder static subscriptions, no atomic cut). Suite counts now 164 runtime + 336 tg + 486 ass. backup/pre-intrinsic-amend holds the pre-rewrite tip.

Compliance benchmark re-acceptance SKIPPED by user decision 2026-08-10 ("don't got 85min, idc") — the shape-C mcap decode path ships on unit-suite + live-smoke evidence only; residual risk lives in eval reconstruction, not runtime. Remaining: land to jazzy (fast-forward both repos), NEVER push without explicit ask.

Open after merge: first live benchmark run is W5's real acceptance (mcap decode path unverifiable statically), event-stream flood on continuous states during transitions (F2, revisit in M2), M4 articulated tier incl. reserved ASA-facet-to-preset auto-attach bridge, ped runtime semantic reactivity (post-ICRA spike, humansim approval gate).

Related: [[asa-semantic-annotations]] (different feature: GCS model tags).
