---
name: urbanverse-feedback-triage
description: Triage verdicts on the 5-item urbanverse/newton feedback report (2026-07-29)
metadata: 
  node_type: memory
  type: project
  originSessionId: 5b146bfa-d99b-44d1-9e82-18af70205649
  modified: 2026-07-29T20:52:09.606Z
---

Teammate's 5-item report triaged 2026-07-29. Merited: (1) newton+urbanverse robot vanish/prep-hang, deep-dive 2026-07-29 CONFIRMS user's load-dependent-timeout diagnosis. CORRECTION: prespawn is NOT z=-5 (d=-5 in _initialpose_generator is per-robot Y stacking); robots stage at ground level just OUTSIDE map bbox where no floor collider exists (newton doesn't hold the ground plane), free-falling through the whole unpause_window bringup, newton bounces snap back to authored pose (sawtooth). geom.move (geom.py:241-286) never writes velocities on EITHER engine, so teleport preserves fall velocity -> tunneling (nconmax 400, no CCD) = disappear. ALL spawn-path timeouts are wall-clock (_CONTROLLER_ACTIVE_TIMEOUT 60s warn-and-proceed, _CONTROLLER_MANAGER_GRACE 30s silent, SpawnUrdf result NEVER checked isaac_simulator.py:282-296) while /clock freezes during bounce holds; sim-conditioned waits (await_sim_step, wait_until_ready, nav2 bt_node_path poll, IsaacHost.unpause call_forever) have NO timeout = stuck variant, unpause can wedge arena_node loop for ALL envs. Confirming log lines: 'controllers not active within budget, proceeding', 'arena: newton rebuild pending, bouncing timeline', 'Service call to /isaac/SpawnUrdf timed out'. REVISED after user pushback (single-robot repro): free-fall downgraded — ground plane collider is an infinite physics plane (size=100 only sizes the visual quad) and jackal newton runs held at prespawn; two vanish causes were ALREADY FIXED 2026-07-21 (a24e3dd sanitize_asset strips scene RigidBodyAPI newton stomped via fabric, 41c697e USD+view teleport writes); ask reporter which build. Still open: unchecked SpawnUrdf result, unbounded silent waits. MIGRATION PLAN agreed (stall-based, no proceed-on-timeout): unbounded LOUD waits inner layers (10s cadence), liveness not deadlines, ONE stall watchdog at benchmark runner keyed on subprocess-log activity (bounds silence-between-progress-events, not total duration; heartbeat-silence!=wedged since isaac main loop legitimately blocks in sync loads). Adversarial findings: controller-less robots legit (dummy sim) so wait_controllers_active needs a controllers-expected flag from bringup, NOT timeout inference; watchdog must land before hang-semantics; teardown stays bounded; SpawnUrdf response is `string path` only (None-check catches timeout+crashed handler only). SPIKE 0+3 STAGED 2026-07-29 (Arena repo): await_loudly/poll_loudly on AsyncNode + ClientWrapper.call_loud + loud ensure(None) branches (Async.py), _call_lifecycle timeout=None -> call_loud, wraps at await_sim_step/nav2 bt poll/unpause_window ACQUIRE/IsaacHost.unpause/_activate_env/spawn_ready (cancellation-aware), wait_controllers_active UNTOUCHED (carve-out until flag), teardown untouched; tests utils/arena_rclpy_mixins/tests/ros/test_loud_waits.py (4 pass in-container). Remaining phases: runner stall watchdog -> proceed-on-timeout kill (SpawnUrdf check, controllers flag) -> isaac frame heartbeat (auxiliary only). (2) Isaac lidar points have NO intensity field (RtxLidarROS2PublishPointCloud is xyz-only). FIXED 2026-07-29 (staged in arena_isaac): points writer replaced by _PointsPublisher in lidar.py, GMO annotator readback (generic-model-output, parse_generic_model_output_data, spherical az/el in DEGREES + dist, scalar = core intensity not aux-gated) publishing xyzi PointCloud2 via rclpy on a render callback, stamped from get_simulation_time_monotonic. CRASH on first live run 2026-07-29: LidarSensor.get_data('generic-model-output') hands the C++ parser an unvalidated aliased buffer (attach uses do_array_copy=False + a 'cuda' device request on a cpu annotator), segfault in _rtx_sensors_gmo _M_realloc_insert. NVIDIA's doctest for that call is +NO_CHECK (never executed), do not trust it. Crash saga (all numpy array_copy segfaults): (a) LidarSensor.get_data pull, (b) owned cpu+copy annotator on render callback, (c) Replicator Writer with cpu annotators, headless, AFTER successful publishes. Root cause model: cpu annotator wrapper goes stale when the variable-size lidar buffer reallocs (per-frame N varies, 322k-362k for jackal planar lidar), next host copy reads freed memory, happens in rep's OgnWriter materialization before write(). NVIDIA's CI test never hits it (tiny constant-size cloud). ALL python-readback variants crashed eventually: cpu writer (runs 3+5, gate annotator did NOT help), cuda annotator = NVIDIA AnnotatorCache KeyError every frame. VERDICT: python may never touch RTX sensor buffers on this build. FINAL FIX (run 7, verified live): the 6.0 C++ ROS2PublishPointCloud node natively supports per-point aux fields, `outputIntensity=True` kwarg on the stock RtxLidarROS2PublishPointCloud attach_writer is the ENTIRE fix (extract node's intensityPtr auto-wires by name), xyzi step 16 verified on jackal (which is a 16-beam 3D lidar in arena, NOT planar, jackal_default/lidar vertical samples 16). Same flags exist for timestamp/normals/objectId if ever needed. Lesson: check the C++ node's .ogn input list FIRST before building python readback. Staged diff vs HEAD = outputIntensity kwarg + destroy() fix (detach_writers never existed in 6.0 API). GZ parity (settled with user): field exists but values constant (no laser_retro authored, gz passes retro through unattenuated). Agreed: NO flat default retro, NO radiometric shim (modeled intensity is geometry-redundant for a learned detector, only material reflectance adds signal). If gz eval ever needed: class-tiered retro at the two SDF chokepoints (gazebo.launch.py world tempfile write + gazebo_simulator.py entity_factory.sdf), same semantic taxonomy as Isaac tier-2 nonvisual materials. Verified working end-to-end in run 2 (GMO variant): xyzi fields on <ns>/lidar/points, frame env_0/jackal/lidar_link, intensity varying (GMO scalar ~1e-6..3e-5 raw, extract 'intensity' ~1e-6..5.4 different scaling). Debug loop: headless `source arena -c 'arena launch sim:=isaac isaac.physics:=newton robot:=jackal headless:=true'`, world default map_empty (arena_hospital_small does NOT exist, wedges spawn silently), instrumentation to /opt/arena_ws/log/lidar_points_debug.log, ~5 min to first lidar frames. Success bar = 10+ min sustained publishing. Tier 2 (meaningful values) = nonvisual sensor materials, not started. CAUTION: runtime Isaac is the 6.0.0 docker image (nvcr isaac-sim:6.0.0), host /home/arena/isaac is 5.1.0-rc and NOT the runtime, verify APIs in the container (docker cp works on the stopped container, aborts on some symlinks). In 6.0 the old detach_writers() never existed, destroy now relies on _SensorRuntime.__del__. (4) hunav ped-robot force inert: _create_robot_message() hardcodes robot at (0,0) every tick (hunav.py:37), force_factor_robot in task_generator.yaml is dead config, other_force_factor dead code in vendored hunav. Not merited: (3) gz rgbd desync: single rgbd_camera sensor = same stamp/rate, needs repro info. (5) eval NOT wired for newton (default physx everywhere), but run_isaacsim.launch.py drops the physics arg on the native path and benchmark CLI regex rejects dotted keys like isaac.physics:=. RESOLUTION item (1), user-confirmed 2026-07-29: the "robot not spawning" symptom was the robot spawning INSIDE a wall collider and getting physics-ejected off the map (urbanverse scenes derive collision walls from the occupancy map at runtime, so unvalidated/fixed spawn poses can land inside geometry, e.g. origin poses outside the zone bbox). Possible guard if it recurs: validate/snap spawn poses against rendered-map free space before teleport (random TM already samples free space, fixed scenario/default poses do not). See [[scene01-newton-crash-investigation]] [[no-hunav-modifications]].
