---
name: human-contract-v2
description: "Full-DOF human wire contract v2 program (2026-07-30), spec decisions, phase status, regen dependency chain"
metadata: 
  node_type: memory
  type: project
  originSessionId: 51b0895b-63c5-451c-b405-3844d0003d85
  modified: 2026-07-31T11:01:05.695Z
---

User authorized full-DOF human skeleton across the entire ecosystem (RViz, panel, Isaac, wire), executed autonomously 2026-07-30. Spec: scratchpad contract_v2.md (fold into JOINTS.md as SSOT). Decisions made autonomously: 24 joints (v1 20 + r_waist/y_waist + l/r_ankle revolute), shoulder y/p/r unlocked with semantic composition R = Rz(y) * R((0,-1,0),p) * twist(r about limb axis), same sign both sides, no wire mirroring. Ankles positive = dorsiflexion, axis (0,-1,0), limits [-0.9,0.6]. Torso chain body -> r_waist -> y_waist -> waist -> torso (mirrors head triple). p_shoulder advisory lo widened -0.4 -> -1.0. Clip format v2: root_xy_yaw ROS-native (deliberate key rename from student's H3D-ordered root_xz_yaw). Wrists/fingers permanently fixed (no HumanML3D source). Hips already full 3-DOF end to end, unchanged.

DONE 2026-07-30 (all but converter, staged not committed): gait.py 24 names legacy-bit-identical, JOINTS.md v2 SSOT, panel regrouped name-keyed (was 5x4 positional, ankles fell off), test_gait local limits table updated (it is an intentional drift guard). Fork: github.com/ros4hri/human_description humble-devel 9050b42 (2.0.2) vendored at src/deps/human_description, branch arena-v2, 2 local commits, built into container overlay (colcon build --packages-select, shadows apt). rig.py v2 numpy-only, key derivation: ZXZ has two decompositions, flip to (a+pi,-b,c+pi) when canonical first angle negative, keeps a=c=pi/2 on the y=r=0 family (documented in JOINTS.md). BONE_MAP regenerated via tests/peds/test_bone_map_parity.py regen in-container, 8 new axes baked, parity fix: shoulder DOFs must measure the composed arm link (conjugated chain moves constants into per-joint links), _URDF_MEASURE_JOINT alias. Rest-pose axis limitation documented (shoulder yaw/twist blend approximate under flexion, exact for gait). Tests green: 42 task_generator + 88 human_steering + 503 rig + 77 isaac peds. Legacy 16 DOF parity dot=1.00 = v1 rendering provably unchanged.

Converter v2 DONE: scratchpad momask clone convert_v2/ (24-DOF, closed-form spherical shoulder y/p + 1-param twist fit, solver bounds decoupled from advisory limits, project_v1 A/B mode). FK-reconstruction validation: legs/ankles 2-4 cm, arms exact RELATIVE to torso, head/arms 10 cm absolute on deep-fold frames only (1.7-2.1 cm upright) = structural single-torso-segment limit, NOT a bug (renders verified by eye, trajectory/heading clean). Known v3 candidate if deep bends matter: split the fold across two spine stations.

SHIPPED 2026-07-31, user-verified live (RViz peds + panel r_waist wiggle + Isaac possessed ped, all passed by eye): voshch/human_description arena-v2 a2d0e47 (single squashed commit, pinned in arena.repos), arena_isaac 305cc47 "extend bone map to 24-joint contract" on isaac6.0.0, Arena 3ca2914 "extend human skeleton contract to 24 joints" on jazzy (single squashed commit incl. submodule bump + pin), ductaingn/momask branch vova 8b29c59 "add convert_v2" (direct write access, no fork, PR to main still open option for the student). Deslopped pre-push: JOINTS.md header has no version tag, all v1/v2 narration removed repo-wide, project_v1 renamed project_20dof, advisory-limits SSOT is GaitGenerator.LIMITS (Section 2 raw table no longer doubles as advisory since shoulder wire limits diverged). Remaining options, not started: gz clip-export track, upstream ros4hri PR for the waist extension, momask PR vova->main for the student.
