---
name: project-human-manual-steering-plan
description: "Settled 2026-07-18 plan for human:=manual backend + human_steering GUI; zero new IDL, GUI is the motion engine, thin adapter backend"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5ba49527-47d6-4a50-9f6e-500945128630
---

POSSESSION SHIPPED 2026-07-19 on feature/ped-gui (Arena + humansim repos, both branched, uncommitted): human/stream+move moved into BaseHumanSimulator as possession (claim-manifest: presence claims, omission releases instantly, empty batch on last release, 1s timeout = crash fallback only, sim-time lease, copy-on-write substitute at publish_arena_peds + new relay_pedestrian_update wrapper, gait_phase re-stamped from base phase table, gate-open-stamp drops stale batches, dual-key name+sim_path bus index because hunav publishes bare names, hooks _on_stream_merged/_on_ped_released/_on_ped_moved sync-only, possessed_peds() accessor). KEY RENAME: manual->dummy (observable engine-less stage, dummy.py), stub->NoopHumanSimulator (noop.py) registered human:=none, IsaacHumanSimulator + enum ISAAC deleted (zero users verified). GUI: driver.release(), one-shot empty manifest, STOP vs Release verbs, held badges/rings. ENGINE (humansim, was on jazzy): world_state now ingests into the force pool as non-autonomous external entities (registry wire-id->aid, proximity adoption <=1m of unbound robots, 2s expiry, excluded from outbound agent_states to avoid adapter flow-ghosts), fixing the previously DEAD robot-feedback channel; adapter publishes possessed peds on world_state at 10 Hz (radius 0.35). RESIDUAL: robot live-pose streaming still event-driven (dirty only on teleports, mirror expires 2s after), possessed ped that is also an engine agent = ghost+mirror both avoided, ensure()-hang hardening STILL PARKED (never approved), engine 708-test + possession 21-test runs are agent in-container results (sandbox has no rclpy). Live validation pending: stock gazebo (default now arena), possess/release on arena, dummy stage, human:=none.
LIVE-FIX 2026-07-19: driver never stamped human/stream headers, so the possession gate's stale-stamp check silently dropped EVERY batch on every backend (symptom: gazebo+rviz deaf to the panel under dummy AND arena). Fix: stamp batches + empty manifest from the node clock, plugin flips its rqt node to use_sim_time (backend gates in sim time per human.launch.py), backend warns once per gate session on stale drops. Claim made STICKY: Intent.held set by first authoring action (teleop/route/teleport/pose/clip/gaze/state), teleop-deadman expiry and route completion no longer drop the ped from the manifest (arena engine was reclaiming 0.5 s after last keypress), only Release/episode change/despawn drop it, driver.release() = forget intent + last_composed, panel Release also deselects. NEW supervisor knob human.steering:=auto|true|false (auto = panel iff resolved human backend dummy and not headless, true always wins over headless, false never, tests in arena_bringup/tests/unit/test_supervisor_args.py). COLOCATION SHIPPED same day (user-directed engine change: "the internal copy should be forced to where the feedback puts it", then "in sync at all times" = pose AND velocity): adapter publishes possessed peds under the ENGINE's own aid (name->aid inverse of _agent_names, hash fallback for unknowns), fills AgentState.velocity from world-frame twist, FEEDBACK_RATE 10->20 Hz. Engine (humansim, uncommitted on feature/ped-gui, NOTE user committed the earlier belief-layer ingest as 'shush' but pool ingest was NOT at HEAD, agent added the whole mechanism): _ExternalEntity(owned, saved_policy_idx, vel), bind-internal when wire aid exists in _agents (save policy_idx, set -1, teleport each update, restore in place on 2 s lease expiry), _apply_external_velocity_pins reasserts fed vel after planning zeroes non-autonomous rows (called in consume + kinematics phase), outbound agent_states excludes ONLY ghost mirrors (bound stay visible, adapter would delete sim actors otherwise), robot proximity adoption unchanged, cleanup on remove/reset. VERIFIED MYSELF in-container: ingest suite 12/12, full engine suite 810 passed 7 skipped 34 s (container path src/Arena/humansim IS visible in-container, earlier failed attempt was a wrong path). Ghost-waypoint rule: canvas.update_peds sweeps waypoint previews + gaze markers for any ped outside the held set (covers release, episode clear, despawn uniformly).
LIVE-TESTED 2026-07-18, user-validated on gazebo (all reported bugs closed: grid removed, pan past map, gaze seam + mirrored heading, waypoint stable-cursor routes, teleop last-wins chords + WASD alternates + Shift-run at Drive speed floored to run threshold, moonwalk fixed DRIVER-side [driver composes its own gait so backend gait fill never runs for panel peds], Isaac sim_path keying + pedestrian_delete relay [base class already relays pedestrian_spawn, never double-spawn], ghost waypoint/gaze cleanup on external despawn). Snapshot feature REMOVED entirely at user request (write path gone, poses-dir load kept for hand-authored wire jsons). Teleport-drag renamed Teleport. OPEN canvas world-change gaps (user parked 2026-07-19): apply_pending rebuilds statics only from topics that messaged this tick, so a walls-only update (door) erases the objects layer until it republishes (fix = per-topic item lists), and static-topic discovery is attach-time only. OPEN from adversarial review: driven-to-undriven freeze (is_driven omits clip_release, released peds never fall back to gait fill, docs promise otherwise), FK preview uncaught CalledProcessError/PackageNotFoundError + xacro respawn every tick, _spawn_walls_impl replaces so door close wipes wall markers, CUBE static objects invisible on points-only canvas, teleport yaw reset, Driver.tick untested, attach-mode auto-panel gap, license TODO. [[user-keyboard-pair-blocking]].
IMPLEMENTED 2026-07-18 (3 parallel agents, unstaged in working tree, user commits to feature branch themselves; user's staged gait.py/JOINTS.md contract work + arena_isaac submodule untouched). Post-impl fix: node.py `_publish_viz_manifest` had a `human_sim.__class__.DUMMY` gate (missed by all sweeps, crashed at runtime); resolved by DELETING the conditional, Pedestrians/Static viz displays now manifest for every backend since manual keeps the bus live. Only remaining DUMMY enum is arena_runtime SimSimulator (sim axis, correct). Follow-ups parked: arena_humans wire.json export (E), training human:=manual one-liner, ArenaHumanSimulator infinite ensure() hardening, JOINTS.md schema migration after user's contract commit. Mockup artifact: https://claude.ai/code/artifact/76f53254-8c4e-4fd5-9aaa-f9d28bdcbd2f

**Architecture**: GUI = motion engine (hunav-with-a-human shape), streams full-state `Pedestrians` on env-level `human/stream`; `ManualHumanSimulator(DummyHumanSimulator)` = thin adapter: validate names vs KnownObstacles, validate `{base}_{id}` joint names, clamp to `GaitGenerator.LIMITS` (rename from `_LIMITS`), cache, relay via `pedestrian_update`; serves `human/move` (`arena_people_msgs/srv/MovePedestrians`, only name+pose honored, translate to `pedestrian_move(Sequence[DynamicObstacle])` via KnownObstacles); 2 Hz keepalive publishes cache. Zero new IDL. `runtime/require_map` (std_srvs/Trigger) on node tree forwards to `world_manager.require_map_server()`.

**Defaults**: `human` default `arena` (gazebo+isaac), `manual` for dummy sim; manual is explicit opt-in + test fallback. Enum DUMMY→MANUAL; swap sites: constants/__init__.py:16, simulators/human/__init__.py:720, runtime.py:23, task_generator.launch.py:117, human.launch.py SelectAction key, test_constants.py:122, constants/README.md:44, launch/human/README.md:17, BRINGUP.md:87, launch/README.md:26. Training has no `human` override, default flip reaches it (surfaced, not silently applied).

**GUI** `utils/human_steering` (ament_python reduced shim, rqt plugin): driver.py (headless importable motion engine: 20 Hz sim-time tick, waypoint follower 0.2 m arrival, teleop 0.5 s wall-clock deadman, compose slider > gaze > clip > gait, stream only driven peds), clips.py (wire.json from dirname(model_uri) + $ARENA_DATA_DIR/human_poses/ snapshots), fk.py (human-tpl.xacro + rviz_utils.hri.rig, preview = composed joint_state read back from bus), canvas.py (map underlay `P/map` where P = node service ns e.g. /arena/env_0/task_generator_node, static-marker baseline, tools Select/Walk/+Waypoint/Teleport-drag/Gaze, right-click menu). Backend detection = `human/move` service discovery. Resync: re-seed integrator from bus when not driving; clear intents on episode_id change (state/episode under P, deeper KeepLast QoS). Dropped: all intent srvs, QueryClips, BACKEND-side per-ped cmd_vel. Reinstated 2026-07-18: DRIVER-side per-ped cmd_vel (`<env>/human/<token>/cmd_vel`, Twist, sanitized token, feeds teleop mode + deadman in the GUI driver, zero backend code) for rqt_robot_steering parity; canonical plan copy is the local scratchpad file / this memory, not the artifact.

**Content**: gestures are bundle clips (arena_humans recipes, CMU BVH). [[project-arena-humans-pipeline]] E-deliverable (separate PR): wire.py projection rig→20 semantic DOF at 30 Hz (anatomical shoulders, root stripped except root_z), hook in bundle.assemble → clips/wire.json; validation = projected walk.bvh must reproduce JOINTS.md gait relationships; curated CMU gesture takes in shared recipe. wire-clip schema section goes in JOINTS.md (+ fix duplicated half-sentence lines 152-154). gz renders locomotion+animation_state only.

**Constants**: stream 20 Hz depth-1 reliable/volatile, keepalive 2 Hz, blend 0.3 s, run threshold 1.8 m/s, wire rate 30 Hz.

**Execution**: step zero = gait.py LIMITS rename, then B (backend) and C (GUI) parallel Sonnet agents, then D (CLI `_meta/tools/human` cloned from tools/viz + source case, docs), ruff + tests, stage NO commit. Known risk: base `pedestrian_teleport` relays teleports via pedestrian_update (smooth) not pedestrian_move; pre-existing, deliberately untouched. gait.py/JOINTS.md carry uncommitted user work, build on top, never revert.

**Adversarial pass 2026-07-18 (3 agents, plan v2 at https://claude.ai/code/artifact/4779a210-71e7-4a45-ad83-e49389ca87cd)**: wire joint names are BARE on the bus (gait.py emits bare, hri_producer suffixes, working-tree JOINTS.md claiming suffixed contradicts code, user to resolve); adapter must publish FULL-ROSTER deep copies (publish_arena_peds forgets gait phase of absent ids, and in-place fill must not leak into cache), publish BEFORE engine relay (Isaac reads joint_state verbatim); human/move updates `known.obstacle.pose` in place (never forget+create_or_get); gazebo/isaac already default arena at task_generator.launch.py:199 (docs stale incl. BRINGUP.md:62/81/341, human/README.md:27-29, constants/README.md:16), edit :199 dict dummy->manual + runtime.py fallback "manual"; keepalive = asyncio task not create_timer; GUI tick = QTimer calling Qt-free driver.tick (rqt_robot_steering precedent in deps); GUI self-teleport must re-seed own integrator; reset-window stream gate (names persist across episodes); rqt needs package.xml <export><rqt_gui plugin> + share/plugin.xml data_file (no in-repo precedent); add pkg to root pyproject ruff src; snapshots $ARENA_DATA_DIR/peds/poses/; tests split tests/ + tests/ros/; E step 0 = reconcile bundle.py .dae copy vs shipped .bvh clips, audit 14 unwired cmu takes first. Pre-existing landmine surfaced: ArenaHumanSimulator.setup() infinite ensure() wait hangs env if humansim engine absent (hardening = separate follow-up).
