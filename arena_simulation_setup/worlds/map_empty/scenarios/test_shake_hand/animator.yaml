name: animator_shake_hand
mode: behavior_tree
extends: adult

initial_sequence: anim_seq

sequences:
  anim_seq:
    steps:
      do_anim:
        attention: {clip: shake_hand}
        duration: {mean: 5.0, std: 0.0}
    then: anim_seq
