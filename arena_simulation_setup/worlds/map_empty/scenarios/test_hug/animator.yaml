name: animator_hug
mode: behavior_tree
extends: adult

initial_sequence: anim_seq

sequences:
  anim_seq:
    steps:
      do_anim:
        attention: {clip: hug}
        duration: {mean: 5.0, std: 0.0}
    then: anim_seq
