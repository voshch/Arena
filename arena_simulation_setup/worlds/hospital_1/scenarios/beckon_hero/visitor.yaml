name: visitor
mode: behavior_tree
extends: adult
initial_sequence: seq
sequences:
  seq:
    steps:
      listen:
        attention:
          gaze: guide
        duration: {mean: 1000.0, std: 0.0}
    then: seq
