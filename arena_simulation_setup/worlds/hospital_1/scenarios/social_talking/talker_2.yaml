name: talker_2
mode: behavior_tree
extends: adult

initial_sequence: talk_seq

sequences:
  talk_seq:
    steps:
      walk_to_meet:
        kind: go_to
        target_pose: {x: 10.5, y: 17.0}
      do_talk:
        interaction: TALK_TO
        attention:
          gaze: partner
        duration: {mean: 1000.0, std: 0.0}
    then: talk_seq
