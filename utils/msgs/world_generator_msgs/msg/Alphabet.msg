# the glyph table, latched so editors never hardcode it
AlphabetEntry[] entries
# shorthand characters, each carrying the arms of the glyph it normalizes to
AlphabetEntry[] aliases
# characters that mean an empty cell
string void_chars
